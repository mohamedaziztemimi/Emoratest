name: CI

on:
  push:
    branches: [main, staging, develop]
  pull_request:
    branches: [main, staging, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Job 1: Lint + Test ──────────────────────────────────
  test:
    name: Lint & Test
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            backend/pyproject.toml
            ml/pyproject.toml

      - name: Install backend deps
        run: cd backend && pip install -e ".[dev]"

      - name: Install ML deps
        run: cd ml && pip install -e ".[dev]"

      - name: Ruff lint — backend
        run: cd backend && ruff check .

      - name: Ruff lint — ML
        run: cd ml && ruff check .

      - name: Pytest — backend
        run: cd backend && pytest -v --tb=short

      - name: Pytest — ML
        run: cd ml && pytest -v --tb=short || [ $? -eq 5 ] && echo "No ML tests yet — skipping"

  # ── Deploy jobs disabled until Railway is configured (Sprint 7) ───
  # deploy-staging:
  #   name: Deploy → Staging
  #   needs: test
  #   if: github.ref == 'refs/heads/staging' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   environment: staging
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Install Railway CLI
  #       run: npm install -g @railway/cli
  #     - name: Deploy to Railway staging
  #       run: railway up --service ${{ vars.RAILWAY_SERVICE_ID }}
  #       env:
  #         RAILWAY_TOKEN: ${{ secrets.RAILWAY_STAGING_TOKEN }}
  #
  # deploy-production:
  #   name: Deploy → Production
  #   needs: test
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   environment: production
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Install Railway CLI
  #       run: npm install -g @railway/cli
  #     - name: Deploy to Railway production
  #       run: railway up --service ${{ vars.RAILWAY_SERVICE_ID }}
  #       env:
  #         RAILWAY_TOKEN: ${{ secrets.RAILWAY_PRODUCTION_TOKEN }}
